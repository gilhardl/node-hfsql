{
  "name": "hfsql",
  "version": "1.0.2",
  "description": "A synchronous interface to use HFSQL database",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepublish": "npm run build",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "hfsql"
  ],
  "author": "GILHARD Lucas <l.gilhard@gmail.com> (http://www.gilhardl.fr)",
  "devDependencies": {
    "typescript": "^2.7.2"
  },
  "dependencies": {
    "odbc": "^1.3.0"
  }
}
